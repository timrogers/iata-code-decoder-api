After making changes, run:

* `npm run prettier-fix` and `npm run eslint-fix` to check for linting issues and automatically fix where possible
* `npm run build` to check that the code builds
* `npm run test` to check that the tests pass